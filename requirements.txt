# MLE Procgen Benchmark — Python 3.10.11 (mandatory: procgen 0.10.7 ships no newer wheel)
# Invoke it as `py -3.10` on Windows, or point any launcher at your 3.10 interpreter.
# CUDA Installation: pip install -r requirements.txt (cu121 index included below)
# Pinned validated versions — see README.md, Section 5 (How to Reproduce)
--extra-index-url https://download.pytorch.org/whl/cu121
procgen==0.10.7
gymnasium[box2d]==1.3.0
gym==0.26.2
stable-baselines3==2.9.0
torch==2.5.1+cu121
numpy==1.26.4
opencv-python==4.8.0.74
matplotlib==3.10.9
tensorboard==2.21.0
pillow==12.3.0
tqdm==4.70.0
cloudpickle==3.1.2
pandas==2.3.3
seaborn==0.13.2
imageio==2.37.4
imageio-ffmpeg==0.3.0
# test runner (tests/)
pytest==9.1.1
# sb3-contrib used in Value vs Policy benchmark (QR-DQN, Section 12 in README):
sb3-contrib==2.9.0
# symbolic regression / system identification (symbolic_regression.py, README section 21).
# scikit-learn is capped at <1.6 because gplearn 0.4.2 calls the estimator helper
# _validate_data, which sklearn removed from its internal API in 1.6: with 1.7 installed
# SymbolicRegressor.fit raises AttributeError instead of searching.
gplearn==0.4.2
scikit-learn==1.5.2

