__pycache__/
.pytest_cache/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/

env/
venv/
.venv/
ENV/
env.bak/
venv.bak/

.vscode/
.idea/
*.swp
*.swo
*~

logs*/

# results/ (figures and videos referenced by the README) and models/ (source
# code) are deliberately tracked.

*.log
*.stdout.txt
*.stderr.txt
# No blanket *.txt here: it silently excluded any text deliverable and kept only
# two whitelisted requirements files. Scratch text output is covered by the
# *.stdout.txt / *.stderr.txt patterns above.

.DS_Store
Thumbs.db

# raw JAX grid output (hundreds of JSONs). The publishable aggregate is versioned:
# jax_port/analysis_full.json + jax_port/cells_summary.json, which reproduce each
# other (tests/test_grid_analysis.py).
jax_port/results_grade/
# follow-ups and partial runs (same rationale; conclusions live in the README)
jax_port/results_followup/
